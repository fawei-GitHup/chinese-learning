# W6-02: 个性化推荐系统 - 实施文档

## 📋 概述

实现了基于用户等级、学习进度、兴趣标签的智能推荐系统，使推荐内容更加个性化和精准。

## ✅ 已完成功能

### 1. 推荐算法引擎

**核心文件**: `lib/recommendations/engine.ts`

**算法特点**:
- **多因子评分系统** (总分 0-100):
  - 等级匹配度 (0-30分): 基于用户HSK等级与内容难度匹配
  - 进度连续性 (0-25分): 根据最近学习活跃度评分
  - 兴趣相关性 (0-20分): 基于用户兴趣标签匹配
  - 新鲜度加分 (0-10分): 推荐用户未尝试的内容类型
  - 多样性加分 (0-10分): 确保不同类型内容混合
  - 热度加分 (0-5分): 基于内容访问量/收藏数

- **惩罚机制**:
  - 已完成内容 (-50分): 降低重复推荐
  - 过难内容 (-20分): 超过用户2个级别
  - 过易内容 (-15分): 低于用户2个级别

- **多样性保证**: 确保不同类型内容(lesson/reading/grammar)均衡分布

### 2. 用户画像构建

**核心文件**: `lib/recommendations/utils.ts`

**用户画像包含**:
- **等级信息**: 当前HSK等级、分级测试分数
- **兴趣标签**: 从学习进度推断的偏好内容类型
- **学习行为**: 
  - 各类型内容完成数量
  - 最近7天活动次数
  - 平均学习时长
  - SRS词汇总数
  - 最后活跃日期

### 3. 内容元数据转换

**支持的内容类型**:
- Lessons (课程)
- Readings (阅读)
- Grammar (语法)
- Medical (医学内容)

**元数据字段**:
- 基础信息: id, type, title, description
- 难度信息: level, difficulty, tags
- 学习信息: estimated_minutes, word_count
- 统计信息: popularity_score, completion_rate

### 4. Dashboard 推荐集成

**修改文件**: `lib/dashboard/api.ts`

**功能**:
- 替换原有简单推荐为智能推荐引擎
- 保留错误回退机制（回退到默认推荐）
- 支持个性化推荐理由显示
- 自动转换推荐结果为Dashboard格式

### 5. 学习路径页面推荐

**修改文件**: `app/(app)/path/page.tsx`

**新增功能**:
- 智能推荐区块：基于当前HSK级别的个性化推荐
- 显示推荐评分（总分）
- 显示推荐理由（emoji + 文本）
- 响应式栅格布局
- 与现有"继续学习"功能共存

## 🏗️ 技术架构

### 目录结构

```
lib/recommendations/
├── types.ts          # 类型定义（用户画像、内容元数据、评分）
├── engine.ts         # 推荐算法核心引擎
├── utils.ts          # 工具函数（画像构建、数据转换）
├── api.ts            # 对外统一API接口
└── index.ts          # 统一导出
```

### 数据流

```
用户请求推荐
    ↓
API入口 (api.ts)
    ↓
获取用户数据 (Supabase)
    ├── 学习进度 (user_progress)
    ├── SRS统计 (user_srs_reviews)
    └── 用户等级 (估算或profile表)
    ↓
构建用户画像 (utils.ts)
    ↓
聚合可用内容 (lessons, readers, grammar)
    ↓
推荐引擎计算 (engine.ts)
    ├── 评分每个内容项
    ├── 应用多样性策略
    └── 上下文特定调整
    ↓
返回推荐结果
```

### 推荐算法伪代码

```typescript
function scoreContent(content, userProfile) {
  // 1. 计算基础得分
  let score = 0;
  score += calculateLevelMatch(content.level, userProfile.level);      // 0-30
  score += calculateProgressContinuity(userProfile.behavior);           // 0-25
  score += calculateInterestRelevance(content.tags, userProfile.tags); // 0-20
  score += calculateFreshnessBonus(content.type, userProfile);          // 0-10
  score += calculatePopularityBonus(content.popularity);                // 0-5

  // 2. 应用惩罚
  if (isCompleted(content.id)) score -= 50;
  if (isTooDifficult(content, userProfile)) score -= 20;
  if (isTooEasy(content, userProfile)) score -= 15;

  // 3. 返回非负分数
  return Math.max(0, score);
}

function ensureDiversity(scoredContent, maxResults) {
  const results = [];
  const typeCount = {};
  const maxPerType = Math.ceil(maxResults / 3);

  for (const item of scoredContent.sortByScoreDesc()) {
    if (results.length >= maxResults) break;
    if (typeCount[item.type] < maxPerType) {
      results.push(item);
      typeCount[item.type]++;
    }
  }

  return results;
}
```

## 📊 推荐因子权重设计

| 因子 | 权重 | 说明 |
|------|------|------|
| **等级匹配度** | 30% | 最重要因子，确保内容难度适中 |
| **进度连续性** | 25% | 鼓励持续学习，奖励活跃用户 |
| **兴趣相关性** | 20% | 推荐用户感兴趣的内容 |
| **多样性加分** | 10% | 避免推荐内容过于单一 |
| **新鲜度加分** | 10% | 鼓励尝试新类型内容 |
| **热度加分** | 5% | 参考其他用户的选择 |

## 🎯 推荐场景适配

### Dashboard 场景
- 优先级: 进度连续性 + 等级匹配
- 多样性要求: 高 (60%)
- 推荐数量: 6项
- 特色: 优先推荐未完成内容

### 学习路径场景
- 优先级: 等级匹配 + 系列内容
- 多样性要求: 中 (50%)
- 推荐数量: 4-8项
- 特色: 按当前HSK级别过滤

### 完成内容后场景
- 优先级: 相关性 + 连续性
- 多样性要求: 高 (70%)
- 推荐数量: 3项
- 特色: 完成lesson推荐reading，完成reading推荐grammar

##  🧪 验收测试

### 1. 基础功能测试

| 测试项 | 操作 | 预期结果 |
|--------|------|----------|
| **Dashboard推荐** | 访问 /dashboard | 显示6个个性化推荐，有推荐理由 |
| **学习路径推荐** | 访问 /path，切换不同级别 | 显示适合该级别的智能推荐 |
| **推荐理由** | 查看推荐卡片 | 显示诸如"适合HSK2级别"、"符合您的兴趣"等理由 |
| **多样性** | 查看推荐列表 | lesson/reading/grammar 混合出现 |

### 2. 算法验证

**场景1: 新用户**
```typescript
用户画像:
├── 等级: HSK1
├── 完成数: 0
└── 最近活动: 0

预期推荐:
├── 主要推荐 HSK1 内容
├── 少量 HSK2 内容（挑战）
└── 推荐理由包含"适合初学者"
```

**场景2: 活跃中级用户**
```typescript
用户画像:
├── 等级: HSK3
├── 完成数: 15 lessons, 8 readings
└── 最近活动: 6次/周

预期推荐:
├── HSK3 为主，HSK4 为辅
├── 优先推荐 grammar（新鲜度高）
├── lesson/reading 均衡
└── 推荐理由包含"延续学习进度"、"适度挑战"
```

**场景3: 高级用户**
```typescript
用户画像:
├── 等级: HSK5
├── 完成数: 40+ 内容
└── SRS词汇: 500+

预期推荐:
├── HSK5-HSK6 内容
├── 热门/高质量内容
├── 医学专业内容（如有标签）
└── 推荐理由包含"高级内容"、"广受欢迎"
```

### 3. 边界情况测试

| 场景 | 处理方式 |
|------|----------|
| **未登录用户** | 返回默认推荐（HSK1-HSK2热门内容） |
| **无进度数据** | 估算等级为HSK1，推荐入门内容 |
| **所有内容已完成** | 降低已完成惩罚，仍返回高分内容 |
| **推荐引擎错误** | 自动回退到旧版简单推荐 |

## 📝 API 使用示例

### 在组件中使用推荐

```typescript
import { getDashboardRecommendations } from '@/lib/recommendations'
import useSWR from 'swr'

function MyComponent() {
  const { data: recommendations, error } = useSWR(
    'dashboard-recommendations',
    () => getDashboardRecommendations(6)
  )

  return (
    <div>
      {recommendations?.map(rec => (
        <RecommendationCard
          key={rec.content.id}
          content={rec.content}
          score={rec.score}
          reason={rec.reason}
        />
      ))}
    </div>
  )
}
```

### 直接调用推荐引擎

```typescript
import { createRecommendationEngine, buildUserProfile } from '@/lib/recommendations'

// 构建用户画像
const userProfile = await buildUserProfile({
  user_level: 'HSK3',
  progress_list: progressData,
  srs_stats: srsData,
})

// 创建引擎并生成推荐
const engine = createRecommendationEngine({
  max_results: 10,
  diversity_threshold: 0.6,
})

const output = engine.generate({
  user_profile: userProfile,
  available_content: allContent,
  completed_content_ids: completedIds,
})

console.log(output.recommendations)
```

## 🔄 未来优化方向

### 短期优化 (1-2周)
- [ ] 添加推荐内容点击率追踪
- [ ] 基于点击率优化推荐权重
- [ ] 支持用户手动设置兴趣标签
- [ ] 添加"不感兴趣"反馈功能

### 中期优化 (1个月)
- [ ] 引入协同过滤算法（基于相似用户）
- [ ] 支持时间因素（早上推荐听力，晚上推荐阅读）
- [ ] 添加学习目标导向推荐（考试备考、商务中文等）
- [ ] 推荐结果A/B测试框架

### 长期优化 (3个月+)
- [ ] 机器学习模型训练（基于历史数据）
- [ ] 实时推荐（基于WebSocket）
- [ ] 跨设备推荐同步
- [ ] 社交推荐（好友在学什么）

## 📚 相关文件

### 新增文件
- `lib/recommendations/types.ts` - 类型定义
- `lib/recommendations/engine.ts` - 推荐算法引擎
- `lib/recommendations/utils.ts` - 工具函数
- `lib/recommendations/api.ts` - 统一API接口
- `lib/recommendations/index.ts` - 统一导出
- `docs/W6-02-recommendation-implementation.md` - 实施文档

### 修改文件
- `lib/dashboard/api.ts` - 集成新推荐引擎
- `app/(app)/path/page.tsx` - 添加智能推荐区块

### 依赖文件
- `lib/progress/api.ts` - 获取用户学习进度
- `lib/srs/api.ts` - 获取SRS统计
- `lib/web-mock.ts` - 内容数据源
- `lib/dashboard/types.ts` - Dashboard类型

 ## 🎯 建议 Commit Message

```
feat(W6-02): 实现个性化推荐系统

- 新增推荐算法引擎（多因子评分 + 多样性保证）
- 创建用户画像构建系统（等级、兴趣、行为）
- 集成到Dashboard和学习路径页面
- 支持上下文特定推荐（Dashboard、Path、完成后）
- 提供统一API接口和工具函数

推荐因子：
✅ 等级匹配度 (30%)
✅ 进度连续性 (25%)
✅ 兴趣相关性 (20%)
✅ 多样性加分 (10%)
✅ 新鲜度加分 (10%)
✅ 热度加分 (5%)

验收标准：
✅ 推荐内容基于用户等级
✅ 推荐内容基于学习进度
✅ 推荐内容多样化（不同类型混合）
✅ 显示个性化推荐理由
✅ 支持错误回退机制
```

## 📖 参考资料

### 推荐系统理论
- [推荐系统实践](https://book.douban.com/subject/10769749/)
- [Collaborative Filtering](https://en.wikipedia.org/wiki/Collaborative_filtering)
- [Content-based Filtering](https://en.wikipedia.org/wiki/Recommender_system#Content-based_filtering)

### 实现参考
- [Netflix推荐系统](https://netflixtechblog.com/netflix-recommendations-beyond-the-5-stars-part-1-55838468f429)
- [YouTube推荐算法](https://research.google/pubs/pub45530/)
- [Spotify Discovery算法](https://engineering.atspotify.com/2020/01/16/for-your-ears-only-personalizing-spotify-home-with-machine-learning/)

## ✨ 亮点功能

1. **智能评分系统**: 6个维度综合评分，权重科学分配
2. **多样性保证**: 确保不同类型内容均衡推荐
3. **上下文感知**: 不同页面应用不同推荐策略
4. **错误容错**: 自动回退到默认推荐，保证用户体验
5. **可扩展设计**: 易于添加新的评分因子和内容类型
6. **类型安全**: 完整的TypeScript类型定义
7. **用户友好**: 清晰的推荐理由，emoji增强可读性
